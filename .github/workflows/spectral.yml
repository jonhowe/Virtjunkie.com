name: Main
on:
  pull_request:
  push:
    branches: [ main ]
    
env:
  SPECTRAL_DSN: ${{ secrets.SPECTRAL_DSN }}

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install and run Spectral CI
      uses: spectralops/spectral-github-action@v3
      with:
        spectral-dsn: ${{ env.SPECTRAL_DSN }}
        spectral-args: $HOME/.spectral/spectral discover github --token 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 -k repo https://github.com/para93/spectral-goat-2024
